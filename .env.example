# Auth.js Configuration
AUTH_SECRET=generate-with-openssl-rand-base64-32
AUTH_TRUST_HOST=true

# Application
NODE_ENV=development

# Note: D1 and KV bindings are configured in wrangler.toml
# Local development uses Miniflare for D1/KV emulation
